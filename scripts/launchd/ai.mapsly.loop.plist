<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>ai.mapsly.loop</string>

    <!-- TCC sidestep: launchd invokes a small bash shim at ~/.mapsly/runner.sh
         which exec's the claude CLI directly. Since `claude` already has Full
         Disk Access (granted via System Settings), claude inherits TCC and
         can read everything under ~/Documents/. The bash shim only touches
         ~/.mapsly/ so it doesn't trip TCC itself. See INC-21. -->
    <key>ProgramArguments</key>
    <array>
        <string>HOME_PLACEHOLDER/.mapsly/runner.sh</string>
    </array>

    <key>StartInterval</key>
    <integer>300</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>HOME_PLACEHOLDER/.mapsly/logs/launchd.out.log</string>

    <key>StandardErrorPath</key>
    <string>HOME_PLACEHOLDER/.mapsly/logs/launchd.err.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
